#include "main.h"
/**
 * stringlength - length calculator
 * @string: string
 * return: length
 */
size_t stringlength(const char *string)
{
	size_t length = 0;

	while (str[length] != '\0')
	{
		lenght++;
	}
	return (length);
}
